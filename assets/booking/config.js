/* ============================================================
   Sussex Van & Trailer Hire — Supabase connection config
   These two values are SAFE to be public: the publishable key
   only allows what Row-Level Security permits. The secret/service
   key is NEVER placed here — it lives in server env vars only.
   ============================================================ */
window.SUPABASE_URL = 'https://tbfujjnlgtdzazxdiact.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_k3woMe5-kLefFmSvgLAfew_zPF092R1';
